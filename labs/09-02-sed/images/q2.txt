Задание 2 (/labs/09-02-sed/test_q2.sh)

1. У нас здесь проблема с чередованием строк в файле `/home/moon/mixed-book.txt`. 

Строки из разных текстов чередуются, нарушая первоначальный порядок. 
Идёт строка из одного файла, потом из другого и т.д.

2. Мы написали `/home/moon/book-fixer.sh` - это скрипт, который должен решать проблему, но он не работает должным образом. Обнови этот скрипт, как указано ниже.

Добавь команды `sed` в функции `process_even` и `process_uneven`, чтобы:

- При запуске  `/home/moon/book-fixer.sh "/home/moon/mixed-book.txt" "/home/moon/alice-in-wonderland.txt"` 
и дальнейшем применении опции `1` формировался правильный файл `alice-in-wonderland.txt`, состоящий из нечётных строк текста.

- При запуске  `/home/moon/book-fixer.sh "/home/moon/mixed-book.txt" "/home/moon/life-of-pi.txt"`
и дальнейшем применении опции `2` формировался правильный файл `life-of-pi.txt`, состоящий из чётных строк текста.

- В конечном итоге скрипт должен создать два файла: 
`/home/moon/life-of-pi.txt` и `/home/moon/alice-in-wonderland.txt` с соответствующим содержимым.
