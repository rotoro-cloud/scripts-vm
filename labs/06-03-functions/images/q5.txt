Задание 5 (/labs/06-03-functions/test_q5.sh)

1. Создан файл `func-to-func.sh`. В файле объявлены две функции. 
Первая пишет свое имя, вторая запускает саму себя, если случайное число меньше, чем аргумент, полученный из родительской функции.

По логике скрипта функция `wrapper` должна запускаться столько раз, сколько указано в $1, а функция `worker` столько раз, сколько в $2. Сейчас это не работает. 

Пример запуска: `./func-to-func.sh 3 3`
