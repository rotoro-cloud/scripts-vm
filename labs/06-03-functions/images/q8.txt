Задание 8 (/labs/06-03-functions/test_q8.sh)

1. Улучшим скрипт `prepare-and-deploy`, чтобы он использовал функцию. Скрипт доступен в `/home/moon`.

Перемести весь код, связанный с последовательностью запуска сборки, в функцию с именем `make_build`. 
Затем вызови функцию и передай аргумент командной строки `$1` в качестве аргумента функции, чтобы оно запустилось в формате `prepare-and-deploy db-subsystem`.
