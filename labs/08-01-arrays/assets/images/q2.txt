Задание 2 (/labs/08-01-arrays/test_q2.sh)

1. В домашнем каталоге `/home/moon/` есть скрипт `shopping-list.sh`. При его запуске предлагается ввести элементы, которые сохраняются в массиве.

Измени скрипт `shopping-list.sh`, чтобы после окончания ввода он печатал `все` элементы `массива` в строке после фразы `Current shopping list is:`

При тестировании имей в виду, что скрипт ожидает слово `save` для окончания ввода.
